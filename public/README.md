Public folder

Place static assets that should be served as-is by Vite here.

- To use your logo, copy the provided image file to `public/logo.png`.
- A placeholder `logo-placeholder.svg` is included so the header shows something by default.
